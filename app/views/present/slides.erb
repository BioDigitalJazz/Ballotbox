<h1><%= @survey_feedback.option_id %></</h1>

<% @slides.each do |slide| %>
  <div class="slide">
    <h3>
      <a href="/present/<%= slide.id %>"><%= @slides.title %></a> 
      <small><%= @slide.description %></small>
    </h3>
   <% end %>
<div class="row">
  <div class="col-md-6 col-md-offset-3 text-center">
      <a href="/present/<%= slide.id %>"><%= @slides.title %>
        NEXT
      </a>
    </div>
</div>

